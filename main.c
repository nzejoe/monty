#include "monty.h"
bus_t bus = {NULL, NULL, NULL, 0};

/**
 * main - main code interpreter
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
	return (0);
}
